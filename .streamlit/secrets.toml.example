# Copy this file to secrets.toml (same folder). Do NOT commit secrets.toml to git.

TEAMS_WEBHOOK_URL = "https://your-organization.webhook.office.com/webhookb2/..."

# Optional: per-row OpenAI analyst suggestions in Streamlit (Upload CSV + Attack or disagreement)
OPENAI_API_KEY = "sk-your-openai-api-key-here"
# OPENAI_MODEL = "gpt-4o"
